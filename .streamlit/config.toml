[server]
runOnSave = true

# Темна база (макет); світла — лише коли «Темний інтерфейс» вимкнено.
[theme]
base = "dark"
primaryColor = "#3B82F6"
backgroundColor = "#111827"
secondaryBackgroundColor = "#1F2937"
textColor = "#F9FAFB"
